<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Цвет при фокусе -->
    <item android:color="@color/text_hint" android:state_focused="true"/>
    <!-- Цвет по умолчанию -->
    <item android:color="@color/text_prime"/>
    <!-- Цвет когда поле отключено -->
    <item android:color="@color/text_prime" android:state_enabled="false"/>
</selector>